export const selectAllHomeData = ({home}) => home?.data;

export const selectCurrentHomeData = ({home}) => home?.current;

export const selectAllFavData = ({home}) => home?.favHome;

export const selectUserHomeData = ({home}) => home?.userHome;

export const selectOfferData = ({home}) => home?.offer;

export const selectFavSearchData = ({home}) => home?.favSearch;
